<%= form_for @changeset, @action, fn f -> %>
  <%= if @changeset.action do %>
    <div class="alert alert-danger">
      <p>Oops, something went wrong! Please check the errors below.</p>
    </div>
  <% end %>

  <div>
    <%= label f, :title, class: "base-form__label" %>
    <%= text_input f, :title, class: "base-form__input" %>
    <%= error_tag f, :title %>
  </div>

  <div>
    <%= label f, :url, class: "base-form__label" %>
    <%= text_input f, :url, class: "base-form__input" %>
    <%= error_tag f, :url %>
  </div>

  <div>
    <p><%= label f, :description, class: "base-form__label" %><p>
    <%= textarea f, :description, class: "primary-textarea" %>
    <%= error_tag f, :description %>
  </div>

  <div class="base-form__actions">
    <%= submit "Submit", class: "button button--primary" %>
  </div>
<% end %>
